"""Tools the worker agents give their models (CONVENTIONS.md §3.1, §6).

One module per tool, all behind the `BaseTool` port in `base.py`. Which agent gets
which tools is not decided here — that is `agents/toolsets.py`, in one place (§3.3).
"""
