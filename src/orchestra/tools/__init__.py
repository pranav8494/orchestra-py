"""Tools the worker agents give their models (§3.1, §6).

One module per tool, all behind the `BaseTool` port in `base.py`. Which agent gets which
tools is decided in `agents/toolsets.py`, not here (§3.3).
"""
