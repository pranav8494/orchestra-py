"""Role workers: one per `AgentRole`, all behind the `Worker` port in `base.py`.

`EchoWorker` stands in for roles Phase B has not reached. Real workers (#5-#7) replace
their stubs one at a time by implementing the same port (§6).
"""
