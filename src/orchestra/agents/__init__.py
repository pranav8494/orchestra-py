"""Agents: the orchestrator and the role workers.

Composes `core/`, `tools/`, `providers/` and `prompts/`; imports nothing from `cli/`,
`app.py` or `config.py` — what an agent needs is injected. See CONVENTIONS.md §3.2.
"""
